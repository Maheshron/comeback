const tooltip = document.getElementById('tooltip');

const share = document.getElementById('share');

//share.addEventListener('click',tooltipShow());

function tooltipShow(){
   
    tooltip.classList.toggle('show');
}